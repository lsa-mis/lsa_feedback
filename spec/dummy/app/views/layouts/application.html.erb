<!DOCTYPE html>
<html>
  <head>
    <title>FeedbackGem Test Application</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Include feedback gem assets -->
    <%= feedback_gem %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
